import "invoice.ral"

TxScript Main(invoiceContractId: ByteVec, payFrom: Address, attoAmount: U256) {
  let invoiceContract = Invoice(invoiceContractId)
  invoiceContract.pay{ payFrom -> attoAmount }() 
}
